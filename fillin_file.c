#include <stdio.h>
void using_file(){
	// Declare the number variables
	// Declare the file pointer 
	// Now do the file opening
	// ....
	// Now modify the rest below to read from the input file 
	// and write to the output file
	scanf("%d",&n, &m);     
	avg = (n + m ) / 2.0 ;
	printf("The average  is %f\n",avg);

	// Make sure the input file exists before running this program
}
void main(){
	using_file();
}
