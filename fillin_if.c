#include <stdio.h>
/* This program reads three numbers and checks if
 * the second number is between the first and the third
 */

int main() {
	// Declare 3 integers a,b and c

	printf("Give me 3 numbers ");
	scanf();  // read three integers call them
	
	if ( a > b ) {
		// if a > b then check b > c and decide to print "YES" or "NO"
	} else {
		// if a < b then check b < c and decide to print "YES" or "NO"
	}
}
