#include <stdio.h>
/* This program reads a number of numbers and sums them up until the sum
 * exceeds 100
 */

int main() {
	// Declare the number of numbers "n" 
	// Declare the individual numbers to read
	// Declare the sum
	printf("How many numbers ? ");
	scanf();  // read three integers call them
	printf("Give me the numbers  one by one ");
	// initialize ??
	while ( )  {   // get the next number until this condition is met
		scanf(); // read the number
		// add it to the sum
	}
	// print the sum
	
}
