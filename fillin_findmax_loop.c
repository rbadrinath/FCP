#include <stdio.h>
// PURPOSE:
// Keep reading numbers and adding them 
// together until the sum is greater than 100
// At that point print the sum and exit
void main(){
	// Declarations
	// initialize the sum to be 0
	// in a loop (for ? while ? do-while ?) do the following
	// read the next number
	// add it to the sum
	// repeat if sum is <=100
	// 
	// Once done with the loop find the average and print it
}
